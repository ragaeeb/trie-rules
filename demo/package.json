{
  "dependencies": {
    "preact": "^10.28.3",
    "trie-rules": "^3.3.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.3",
    "@types/node": "^25.2.3",
    "typescript": "~5.9.3",
    "vite": "^8.0.0-beta.13"
  },
  "name": "demo",
  "overrides": {
    "vite": "^8.0.0-beta.13"
  },
  "private": true,
  "scripts": {
    "build": "tsc && vite build && cp CNAME dist/",
    "deploy": "bun run build && surge ./dist --domain trie-rules.surge.sh",
    "dev": "vite",
    "preview": "vite preview"
  },
  "type": "module",
  "version": "0.0.0"
}